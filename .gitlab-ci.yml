stages:
  - build
  - test

variables:
  GOCACHE: /go/cache

go-build:
  stage: build
  image: golang:1.23.9
  tags:
    - students-buildkit-medium
  script:
    - git fetch origin $BRANCH
    - git checkout $BRANCH
    - go mod download
    - go mod tidy
    - mkdir -p $GOCACHE
    - go build -o auth ./go/cmd/auth
  cache:
    paths:
      - /go/cache/

